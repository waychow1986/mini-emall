server:
  port: 8081
spring:
  application:
    name: emall-order
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      discovery:
        metadata:
          gRPC_port: 9091
      config:
        enabled: true
        format: yaml
grpc:
  server:
    port: 9090