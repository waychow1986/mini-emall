server:
  port: 8080
spring:
  profiles:
    active: dev
  application:
    name: emall-web
---
spring:
  profiles: dev
  cloud:
    consul:
      host: 20.171.143.16
      port: 8500
      discovery:
        prefer-ip-address: true
        ip-address: 4.242.239.19
      config:
        enabled: true
        format: yaml
---
spring:
  profiles: prod
  cloud:
    consul:
      host: consul-server
      port: 8500
#      discovery:
#        prefer-ip-address: true
#        ip-address: 4.242.239.19
      config:
        enabled: true
        format: yaml