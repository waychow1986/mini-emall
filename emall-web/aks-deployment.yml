kind: Deployment
apiVersion: apps/v1
metadata:
  name: emall-web
  namespace: consul
spec:
  replicas: 2
  selector:
    matchLabels:
      app: emall-web
  template:
    metadata:
      labels:
        app: emall-web
    spec:
      containers:
        - name: emall-web
          image: acreus2euap.azurecr.io/emall-web/emall-web:v0.1
          resources:
            limits:
              cpu: 1
              memory: 256Mi
            requests:
              cpu: 1
              memory: 256Mi
          imagePullPolicy: IfNotPresent
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext: {}
      schedulerName: default-scheduler
---
kind: Service
apiVersion: v1
metadata:
  name: emall-web
  namespace: consul
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: emall-web
  type: LoadBalancer